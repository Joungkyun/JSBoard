##### JSBOARD + OOPS administartion Center #################################
#
#  Original Developer	: 김병찬 < redhands@linux.sarang.net >
#
#  Maintainer		: 방창현 < winchild@kldp.org >
#  개발 version corder	: 김정균 < admin@oops.org >
#  안정 version corder	: 차경묵 < dimanche@dimanche.co.kr >
#
#  CVS Server 관리	: 김영민 < blhole@hotmail.com >
#  Homepage 공간 제공	: http://www.kldp.org
#
#############################################################################
#
#                               Installation in JSBoard 1.0 v0.3
#
#############################################################################

작성 : 김정균 <admin@oops.org>
일시 : 2000.6.16

이 문서는 JSBoard 1.0 version의 설치를 위해 제공을 한다.
이 문서로 인한 피해나 JSBoard의 사용으로 인한 피해에 대해서는
아무런 책임을 질수 없으니 신중하게 판단하고 설치를 시도하도록
하기를 바란다.


  ##  좀더 자세한 설명을 보기를 원하거나 Installer가 아닌 ##
  ##  수동 설치를 원하면 QUICK_INSTALL 문서를 참조 하라   ##


******* 주의사항 ***********************************************************

하나.
JSBoard 1.0은 PHP4 기반하에서 정상작동을 하며, Session 기능을
compile 시에 꼭 넣도록 해야 한다.

하나.
계정 유저들과 0.7 이하 버젼에서의 upgrade를 하는 유저들은 QUICK_INSTALL 를
봐야 한다.

하나.
0.7 이하 버젼과 DB 구조체와 SQL query의 형식이 변경이
되었으므로 무조건 덮어 씌우지 말기 바란다.

****************************************************************************


**** 목 차 ****

  1. 사용환경
    1-1. exec() 함수 test
    1-2. MySQL 함수 test
    1-3. MySQL mysql database db table 확인
    1-4. web server 설정
  2. JSBoard 설치
  3. JSBoard 운영
  4. UPGRADE
    4-1. 0.8 또는 1.0 prelease version에서의 upgrade
    4-2. 0.7 이하 버젼에서의 upgrade
  5. 알려진 버그

***************

1. 사용환경

  apache 1.3.x 이상
  MySQL 3.22.30 이상 (RPM 설치 권장 - ftp://ftp.oops.org)에서 받을수 있음
  PHP-4.0.0 이상(RPM 설치 권장 - ftp://ftp.oops.org)에서 받을수 있음

  * 체크 사항 *

  서버의 php가 exec() 함수와 MySQL 함수가 지원되는지 확인한다.

  1-1. exec() 함수 test

    일단 다음과 같은 파일을 하나 만든다. test.php3라고 칭하겠다.

    <?php
    echo("## exec() 함수 테스트  ##<p><br>\n\n");
    exec("echo \"exec()함수가 정상 작동 합니다\"", $test);

    echo $test[0];
    ?>

    파일을 만든 다음 웹상에서 위의 파일을 호출했을 경우

    -------------------------------------------------
    ## exec() 함수 테스트 ##

    exec()함수가 정상 작동 합니다
    -------------------------------------------------

    가 출력이 되어져야 한다.

  1-2. MySQL 함수 test

    info.php3 라는 파일을 아래와 같은 내용으로 생성한다.

    <?php
    phpinfo();
    ?>

    그리고 웹상에서 호출을 했을 경우 MySQL에 대한정보가 있는지 확인한다.
    단 주의 할것은 MySQL의 정보가 있더라도 php 설치시에 mysql의 경로를
    지정해 준것과 실제 MySQL의 경로가 틀릴 경우에는 접속 에러가 발생할수
    있다. 가장 대표적인 경우가 PHP는 rpm으로 설치를 하고 MySQL은 source
    compile을 했을 경우이다. PHP를 rpm으로 설치를 할경우에는 거의 대부분이
    /usr/lib/mysql 에 위치하는 경우이기 때문이다.

  1-3. MySQL mysql database db table 확인

    MySQL을 처음 설치해본 사람들은 Quick_INSTALL 문서의 MySQL부분을
    참조하도록 한다. 중요한 것은 Installer를 사용하기 위해서는 DB 구조체를
    미리 만들어 놓아서는 안된다. root password만 지정하기를 바란다.

    일단 MySQL로 root의 권한으로 login을 한 다음 mysql database의 db table이
    아래와 동일한지 확인을 한다.

    mysql> use mysql;
    mysql> desc db;

    +-----------------+---------------+------+-----+---------+-------+
    | Field           | Type          | Null | Key | Default | Extra |
    +-----------------+---------------+------+-----+---------+-------+
    | Host            | char(60)      |      | PRI |         |       |
    | Db              | char(32)      |      | PRI |         |       |
    | User            | char(16)      |      | PRI |         |       |
    | Select_priv     | enum('N','Y') |      |     | N       |       |
    | Insert_priv     | enum('N','Y') |      |     | N       |       |
    | Update_priv     | enum('N','Y') |      |     | N       |       |
    | Delete_priv     | enum('N','Y') |      |     | N       |       |
    | Create_priv     | enum('N','Y') |      |     | N       |       |
    | Drop_priv       | enum('N','Y') |      |     | N       |       |
    | Grant_priv      | enum('N','Y') |      |     | N       |       |
    | References_priv | enum('N','Y') |      |     | N       |       |
    | Index_priv      | enum('N','Y') |      |     | N       |       |
    | Alter_priv      | enum('N','Y') |      |     | N       |       |
    +-----------------+---------------+------+-----+---------+-------+
    13 rows in set (0.00 sec)

    mysql>

    만약 위와 같은 결과가 나오지 않는다면 (즉 column 수가 동일한지 확인을 한
    다) jsboard/Install/act.php3 의 37번째 라인의 query에서 컬럼수대로 수정
    을 해 줘야 한다.

  1-4. web server 설정

    jsboard의 index file은 index.php3 이다 그러므로 httpd.conf 에서
    DirectoryIndex 지시자에 index.php3를 추가해 주도록 한다. 또한 ph file
    들의 보안을 위해서 httpd.conf 의

    AddType application/x-httpd-php3 .php3

    설정에 .ph 를 추가해 주도록 한다.

2. JSBoard 설치

  ******* 주의사항 *********************************************************

  계정 유저들은(즉 계정에 설치를 하려는 사람들은) Installer를 사용하지
  않는다. QUICK_INSTALL 를 참조하기 바란다. Root의 권한을 가지고 있는 사람
  들도 Installer로 설치가 제대로 되지 않을 시에는 QUICK_INSTALL 를 참조하여
  설치를 해보도록 하기 바란다.

  여기서는 Root 의 권한을 가지고 있는 사람들만 해당한다는 것을 명심하잣!!

  **************************************************************************


  일단 설정 파일들을 위치 시키고 그 파일들의 퍼미션을 조정하도록 한다.
  jsboard/Install/script 에서 아래의 명령을 실행한다. 실행이 되지 않으면
  퍼미션을 755로 수정하기 바란다.

  다시 한번 주의를 주는데, 계정 유저들은 QUICK_INSTALL 를 따라서
  해야 한다!!!!!!!!!!!!

  $ ./root.sh

  실행을 한후에 아무런 메세지가 나오지 않으면 된것이다. 그리고 다시
  jsboard/Install/include/passwd.ph 를 편집한다.

  <?php
  /****************************************************/
  // Specify MySQL Root Password
  // mysql을 관리할 root의 password 를 지정

  $passwd = "MySQL_ROOT_PASSWORD";
  /****************************************************/
  ?>

  위는 passwd.ph 의 내용이며, "MySQL_ROOT_PASSWORD" 부분에 MySQL의
  root password를 넣어 준다.

  다음 웹 브라우져로 http://domain.com/jsboard/Installer 로 접근을 한다.

  -------------------------------------------------------------------------
                             JSBoard Installer
  -------------------------------------------------------------------------

                          [ ] Korean [ ] English

  -------------------------------------------------------------------------
                                 [submit]
  -------------------------------------------------------------------------

  과 같은 화면을 볼수 있을 것이다. 여기서 언어를 선택하도록 한다. 현재는
  국문과 영문만을 지원한다. 선택하고 [submit] 을 click 하도록 한다.


  -------------------------------------------------------------------------
                             JSBoard License
  -------------------------------------------------------------------------

  주절주절주절주절 ...........................................
  주절주절주절주절 ...........................................
  주절주절주절주절 ...........................................

  -------------------------------------------------------------------------
                                  [AGREE]
  -------------------------------------------------------------------------

  언어를 선택하고 나면 위의 JSBoard를 사용하기 위한 License가 나온다. 읽어
  보고 동의를 하면 [AGREE]를 click 한다.


  -------------------------------------------------------------------------
                             JSBoard Installer
  -------------------------------------------------------------------------

                     [ ] First Installation [ ] Upgrade

  -------------------------------------------------------------------------
                                  [submit]
  -------------------------------------------------------------------------

  다음은 처음 설치를 하는 경우와 업그레이드를 하는 겨우를 선택한다. 다만
  Installer 에서는 Upgrade는 지원하지 않는다. 단지 QUICK_INSTALL 문서를
  참조하라는 메세지 만 보여줄 뿐이다. 그럼 First Installation 을 선택하도록
  한다.


  -------------------------------------------------------------------------
                         JSBoard Environment Check
  -------------------------------------------------------------------------

          Jsboard를 사용하기 위한 환경 설정을 검사하고 있습니다
          5초 후에 결과를 보실수 있습니다

  -------------------------------------------------------------------------
                          [ 5초간 기다려 주세요 ]
  -------------------------------------------------------------------------

  새로이 추가된 부분이다. 이 부분에서는 특별히 할것 없이 5초간 기다리기만
  하면 된다. 이 부분에서 OS Type, MySQL연동 체크, db table의 column 수 체
  크, exec() 함수 사용여부, httpd.conf의 index file 설정 여부, jsbaord를
  를 사용하기 위한 권한 설정 여부를 체크를 한다.

  * 주의 할것은 이 부분은 meta tag로 체크후에 결과 페이지로 넘어 가게 되어
    있는데 Linux용 Netscape 는 Meta tag가 제대로 작동을 하지 못하는 경우가
    많다. 이 경우에는 windog에서 install program을 다시 시행 하던지 또는
    QUICK_INSTALL 문서를 참조해서 수동으로 설치를 하기 바란다.

  -------------------------------------------------------------------------
                      JSBoard Enviornment Check Reuslt
  -------------------------------------------------------------------------

  OS Type			: Linux 
  MySQL check			: Failed 

  MySQL login에 실패를 했습니다. jsboard/Installer/include/passwd.ph 에
  MySQL의 root password가 정확한지 확인해 주시고 맞으면 PHP의 설치시에
  --with-mysql 옵션이 들어갔는지 확인해 주십시오
  만약 DB server가 독립되어 있다면 QuickInstall문서를 참조 하여 설치를 하시
  기 바랍니다  

  exec() function check		: OK 
  index file check		: OK 
  Fermission check		: OK 


  -------------------------------------------------------------------------

  -------------------------------------------------------------------------

  위의 화면은 환경 체크 결과를 보여준다. 위에서는 에러가 하나 뜬것을 보여
  주고 있는데 위의 결과가 모두 OK이면 다음 화면으로 넘어가고 위와 같이
  에러가 발생하면 다시 처음 화면으로 가게 된다. 에러가 발생할 경우에는
  위와 같이 점검할 사항들을 제시해 준다.

  * index file check error message에서 httpd.conf file이 여러개가 있다는
    메세지가 나올 경우에는 사용하는 설정파일외에는 다른 파일들의 이름을
    변경해 줘야 한다. 안그러면 index file 설정에 문제가 없을 경우에도
    계속해서 이 메세지가 뜨게 된다.

  -------------------------------------------------------------------------
                             JSBoard Installer
  -------------------------------------------------------------------------

                              MySQL password
                             [              ]

                          Password 초기화 [reset]

  -------------------------------------------------------------------------

  -------------------------------------------------------------------------

  이 화면에서 MySQL의 root password를 넣고선 엔터를 누른다. 아래의 Password
  초기화의 [reset]을 클릭하면 session에 있는 기존의 password를 지워버리는
  역할을 한다.


  -------------------------------------------------------------------------
                             JSBoard Installer
  -------------------------------------------------------------------------

                     DB name [                       ]
                     DB user [                       ]
                     DB pass [                       ]

                               [ E N T E R ]
  -------------------------------------------------------------------------
  DB name과 DB user는 MySQL에 등록이 되어 있지 않은 것을 지정하셔야 합니다.
  -------------------------------------------------------------------------

  JSBoard가 사용할 DB 정보들을 입력한다. 주의 할것은 아래에 적어 놓았듯이
  MySQL에 존재하지 않는 새로운 DB naem, DB user를 기입해야 한다.

  그리고 Enter 를 click 하면, 별다른 에러가 없다면 다음과 같은 경고창이
  뜰것이다.


        등록이 완료 되었습니다.
        Admin Page로 이동을 합니다.
        Admin Page의 초기 Password는
        0000 입니다.


3. JSBoard 운영

  설치가 돤료 되었다면 이제 웹브라우져에서 접속을 해 보도록 한다. 우선 전체
  설정 부터 살펴 보도록 한다.

  http://domain.com/jsboard/admin

  으로 접속을 하도록 한다. 그러면 login 창이 뜰것이다. JSBoard의 기본 패스
  워드는 모두 초기값으로는 "0000" 으로 되어 있으니 이 패스워드로 접속을 하도
  록 한다. (JSBoard의 처음 설치후 JSBoard에서 요구하는 모든 패스워드들은
  "0000"이 기본 으로 되어 있다)

  OOPS Administration Center v1.6 에서 달라진 기능은 일단 예전에 페이지 별로
  합계가 나오던 것을 모든 페이지의 토탈 합계 기능을 추가 하였다. 또한 좌측
  하단에 전역 변수 설정 옵션이 추가 되었으며 각 게시판의 통계 기능이 추가
  되었다. 그외에 검으죽죽한 화면을 게시판의 색상에 맞춰서 나오게끔 최적화를
  하였으며, 그외의 달라진 점은 거의 없다. :-) 써 본 사람들은 잘 사용을 할
  것이다. 좀 신경을 쓴 부분이 있다면 게시판 삭제시에 한번은 물어 보고
  conform을 받은 뒤에 삭제가 이루어 지게 해 놓은 것이다.


4. UPGRADE

  4-1. 0.8 또는 1.0 prelease version에서의 upgrade

    0.8 또는 1.0 prelease 에서의 upgrade 무척 단순하다. 즉, 1.0의 소스를 받
    아서 덮어 씌어 주기만 하면 된다. 소스를 덮어 씌운후에
    jsbaord/Installer/script 에서 root사용자의 경우에는 root_p.sh 를 실행해
    주면 되며, 계정 유저의 경우에는 account_p.sh를 실행해 주기만 하면 된다.

    단 주의 할것은.... PHP3 사용자들은 PHP4로 update를 해 줘야만 한다.!!!!!!

  4-2. 0.7 이하 버젼에서의 upgrade

    현재로서는 0.6 안정 버젼과 0.7 버젼에서만 upgrade가 지원이 된다. 그 이전
    버젼을 사용하는 user들은 최소한 0.6 안정 버젼으로 upgrade를 한후에
    upgrade를 할수가 있다. 자세한 사항은 QUICK_INSTALL 문서에서 다루도록 한다

5. 알려진 버그

글쓰기, 수정 및 관련글 쓰기 화면에서 <textarea> 에 WRAP을 hard로 설정할
시에 auto_link() 함수 - url을 자동으로 링크시키는 함수 - 와 글내용에
table tag를 사용할시에 문제점이 발생할 여지가 있다. 
